/**
 * Phosphor Icons for React Server Components — the same glyphs without the
 * client-side IconContext, so they render in a server component with no
 * `'use client'` boundary.
 *
 *   import { ArrowRight } from 'lightweight-ui/icons/ssr'
 */
export * from '@phosphor-icons/react/ssr'
