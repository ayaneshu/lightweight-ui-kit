/**
 * The kit's icon set: Phosphor Icons (https://phosphoricons.com,
 * github.com/phosphor-icons/react, MIT). Every component draws its glyphs from
 * it, so importing from here keeps your screens in the same visual language.
 *
 *   import { Trash, ArrowRight } from 'lightweight-ui/icons'
 *
 * Tree-shaken per icon — only the glyphs you import ship. Conventions: regular
 * weight, sized to the text beside it (13–18px); `weight="bold"` only for small
 * marks (a tick, a caret) that would otherwise vanish.
 */
export * from '@phosphor-icons/react'
